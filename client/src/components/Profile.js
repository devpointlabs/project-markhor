import React from "react";

import Typography from '@material-ui/core/Typography';

const Profile = () => {
  return (
    <Typography variant="h3" component="h1" underGutter>Profile</Typography>
  );
};

export default Profile;
